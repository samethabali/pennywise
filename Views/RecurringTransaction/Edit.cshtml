@model PennyWise.ViewModels.RecurringTransactionViewModel

@{
    ViewData["Title"] = "Abonelik Düzenle";
}

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="glass-card border-0 shadow-sm p-4">
            <div class="card-header bg-transparent border-0 pt-4 pb-0">
                <div class="icon-badge-3d" style="background: linear-gradient(135deg, #F59E0B 0%, #EF4444 100%); width: 48px; height: 48px; font-size: 1.1rem;">
                    <i class="fa-solid fa-pen-to-square"></i>
                </div>
                <div>
                    <h2 class="mb-0">Abonelik Düzenle</h2>
                    <p class="text-muted small mb-0">Tekrarlayan işleminizin detaylarını güncelleyin.</p>
                </div>
            </div>

            <form asp-action="Edit" asp-route-id="@ViewBag.RecurringId" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

                <!-- Açıklama -->
                <div class="mb-3">
                    <label asp-for="Description" class="form-label"></label>
                    <input asp-for="Description" class="form-control" placeholder="Örn: Netflix Aboneliği, Ev Kirası, Maaş" />
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>

                <!-- Tutar ve Periyot -->
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label asp-for="Amount" class="form-label"></label>
                        <div class="input-group">
                            <input asp-for="Amount" type="number" step="0.01" min="0.01" class="form-control" placeholder="0.00" />
                            <span class="input-group-text">₺</span>
                        </div>
                        <span asp-validation-for="Amount" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="RecurrenceType" class="form-label"></label>
                        <select asp-for="RecurrenceType" class="form-select">
                            <option value="0">🔄 Günlük</option>
                            <option value="1">📅 Haftalık</option>
                            <option value="2">📆 Aylık</option>
                            <option value="3">📋 Yıllık</option>
                        </select>
                        <span asp-validation-for="RecurrenceType" class="text-danger"></span>
                    </div>
                </div>

                <!-- Kategori -->
                <div class="row mb-3">
                    <div class="col-md-12">
                        <label asp-for="CategoryId" class="form-label"></label>
                        <select asp-for="CategoryId" class="form-select" asp-items="ViewBag.Categories">
                            <option value="">-- Kategori Seçin --</option>
                        </select>
                        <span asp-validation-for="CategoryId" class="text-danger"></span>
                    </div>
                </div>

                <!-- Başlangıç ve Bitiş Tarihi -->
                <div class="row mb-4">
                    <div class="col-md-6">
                        <label asp-for="StartDate" class="form-label"></label>
                        <input asp-for="StartDate" class="form-control" type="date" />
                        <span asp-validation-for="StartDate" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="EndDate" class="form-label"></label>
                        <input asp-for="EndDate" class="form-control" type="date" />
                        <small class="text-muted">Boş bırakırsanız süresiz devam eder.</small>
                    </div>
                </div>

                <div class="d-flex gap-3 justify-content-end">
                    <a asp-action="Index" class="btn btn-outline-primary">İptal</a>
                    <button type="submit" class="btn btn-primary fw-semibold">
                        <i class="fa-solid fa-check me-2"></i>Değişiklikleri Kaydet
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var amountInput = document.getElementById('Amount');
            if (amountInput) {
                amountInput.addEventListener('input', function () {
                    if (parseFloat(this.value) < 0) {
                        this.value = '';
                        this.setCustomValidity('Tutar negatif olamaz!');
                        this.reportValidity();
                    } else if (parseFloat(this.value) === 0) {
                        this.setCustomValidity('Tutar sıfır olamaz!');
                        this.reportValidity();
                    } else {
                        this.setCustomValidity('');
                    }
                });
                amountInput.addEventListener('keydown', function (e) {
                    if (e.key === '-' || e.key === 'e') {
                        e.preventDefault();
                    }
                });
            }
        });
    </script>
}
